version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g pnpm
      - pnpm install
  build:
    commands:
      - pnpm run build
      - pnpm run export
artifacts:
  base-directory: out
  files:
    - '**/*'